version: '3.9'
services:
  postgresql:
    image: bitnami/postgresql:latest
    volumes:
      - ./myshop.sql:/docker-entrypoint-initdb.d/myshop.sql:ro
    environment:
      - POSTGRES_PASSWORD=password
    restart: unless-stopped
    ports:
      - 5432:5432